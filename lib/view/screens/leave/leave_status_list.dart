import 'package:flutter/material.dart';

class LeaveStatusList extends StatelessWidget {
  const LeaveStatusList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
       body: Center(
        child: Text("Leave Status List"),
      ),
    );
  }
}