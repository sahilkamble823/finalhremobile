import 'package:flutter/material.dart';

class ExpnseStatusList extends StatelessWidget {
  const ExpnseStatusList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
       body: Center(
        child: Text("Expense Status List"),
      ),
    );
  }
}